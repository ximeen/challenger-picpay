version: '3.8'

services:
  picpay_challanger:
    container_name: picpay_challanger
    image: postgres
    ports:
      - "5432:5432"
    environment:
        - POSTGRES_USER=admin
        - POSTGRES_PASSWORD=admin
        - POSTGRES_DB=pg_picpay
